## 1. 布局与主题

- [x] 1.1 调整 `src/tui/app.py` 的 `compose()` 与 CSS，增加输入框上方的独立 `queue-tray` 区域
- [x] 1.2 把 TUI 主题改为明亮浅绿色视觉方向，并确保主要正文文字使用白色
- [x] 1.3 更新输入框 placeholder 为 `SmartIPO 在线为你解答 ...`

## 2. 聊天与排队渲染

- [x] 2.1 调整主消息区渲染，默认使用 `👨‍💻` / `🤖` 角色标识，移除 `你:`、`AI:`、`EasyHarness:` 等默认前缀
- [x] 2.2 把待处理消息从 timeline 主消息区移出，改为在 `queue-tray` 中以居中泡泡形式展示
- [x] 2.3 弱化方括号与日志式状态串，保留必要的工具/系统信息但提升整体聊天感

## 3. 验证

- [x] 3.1 更新 `test/test_tui_app.py`，验证排队消息不再出现在 timeline 且会显示在独立托盘区域
- [x] 3.2 更新 `test/test_tui_app.py`，验证用户和助手消息默认改为图标式前缀，placeholder 已更新
- [x] 3.3 运行相关 TUI 测试并确认新的视觉语义与排队分区通过验证
