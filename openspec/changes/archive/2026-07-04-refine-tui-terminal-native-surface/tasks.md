## 1. 终端原生底板收敛

- [x] 1.1 调整 `src/tui/app.py` 的 CSS，移除 `Screen`、主容器和大面积基础区域的显式背景色
- [x] 1.2 保留浅绿色作为边框、焦点和局部强调色，并确保正文可读性不下降

## 2. queue tray 按需显示与响应式收缩

- [x] 2.1 调整 queue tray 的显隐逻辑，仅在存在排队消息时显示
- [x] 2.2 移除 queue tray 的空态占位文案与固定最小高度，按内容收缩布局
- [x] 2.3 收紧辅助区域间距，确保 timeline 在不同窗口尺寸下保持更大的可用空间

## 3. 验证

- [x] 3.1 更新 `test/test_tui_app.py`，验证空队列时 queue tray 隐藏且不显示空态文本
- [x] 3.2 更新 `test/test_tui_app.py`，验证有排队消息时 queue tray 显示且 timeline 不被空托盘占位干扰
- [x] 3.3 运行相关 TUI 测试并确认终端原生底板与按需显示行为通过验证
