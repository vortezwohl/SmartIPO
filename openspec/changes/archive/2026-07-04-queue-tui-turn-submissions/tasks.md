## 1. TUI 队列状态建模

- [x] 1.1 在 `src/tui/app.py` 中为提交轮次增加本地 FIFO 队列、active turn 标识和 turn_id 归属校验
- [x] 1.2 把 `on_input_submitted()` 改为入队语义，并在空闲时自动启动队首消息
- [x] 1.3 调整完成、失败和新会话重置路径，确保队列会继续消费且旧轮次事件不会污染当前会话

## 2. 时间线与状态展示

- [x] 2.1 复用用户消息条目展示队列状态，支持处理中、排队中和顺位信息渲染
- [x] 2.2 调整本地 thinking 占位的触发时机，只在当前 active turn 真正开始执行时显示

## 3. 验证

- [x] 3.1 在 `test/test_tui_app.py` 中补充多条提交排队、自动续跑和顺位展示测试
- [x] 3.2 在 `test/test_tui_app.py` 中补充失败后继续消费下一条与新会话忽略旧事件测试
- [x] 3.3 运行相关 TUI 测试并确认串行队列语义通过验证
