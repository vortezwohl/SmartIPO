## 1. 保守静态清理

- [x] 1.1 逐个确认并清理已知真正未使用的 import 或形参，优先采用前导下划线保留签名
- [x] 1.2 保留 `__init__.py` 重导出、抽象接口签名和测试替身签名这类不能机械删除的结构

## 2. 渠道命名收敛

- [x] 2.1 把 `src/model_config.py` 中 DeepSeek 渠道配置键从 `openai` 调整为 `deepseek`
- [x] 2.2 同步更新主脑模型配置与相关测试，确保渠道键名调整后 `model_id` 和 provider 语义保持不变

## 3. 验证

- [x] 3.1 运行编译检查与现有测试，确认静态清理和渠道键名调整没有改变运行行为
- [x] 3.2 运行全文搜索，确认仓库中不再残留把 DeepSeek 渠道键写成 `openai` 的配置引用
